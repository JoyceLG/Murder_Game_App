// Dev defaults: talk directly to the FastAPI dev server (CORS is enabled on the API).
export const environment = {
  production: false,
  apiBase: 'http://localhost:8000',
  wsBase: 'ws://localhost:8000',
};
